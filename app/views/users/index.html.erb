<%= @users.each do |user| %>
  <li>
    <%= link_to user.name, user %>
    <%= user.email %>
    <% if signed_in? && current_user.admin? %>
      <%= link_to 'delete', user, method: :delete,
                                       confirm: 'really, mr.admin?' %>
    <% end %>
  </li>
<% end %>