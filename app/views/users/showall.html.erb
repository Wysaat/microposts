<% provide :title, "users" %>
<% @users.each do |user| %>
	<%= link_to user.name, user %>
	<%= user.email %>
	<% if signed_in? && current_user.admin? %>
	  <%= link_to 'delete', user.delete, method: :delete,
	                                     confirm: 'really?' %>
	<% end %><br>
<% end %>